// Stub — the real atlassian-webresource-webpack-plugin is only needed
// when auto-generating WRM descriptors.  We manage atlassian-plugin.xml by hand,
// so this file is intentionally empty and only exists to keep the config importable.
module.exports = {};
